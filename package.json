{
  "name": "convos-agent-pool-manager",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node --env-file=.env --watch src/index.js",
    "start": "node src/index.js",
    "test": "node --env-file=.env --test src/**/*.test.js",
    "db:migrate": "node --env-file=.env src/db/migrate.js",
    "drain:all": "node --env-file=.env scripts/drain-sprites.js"
  },
  "dependencies": {
    "@fly/sprites": "^0.0.1",
    "express": "^5.1.0",
    "nanoid": "^5.1.5",
    "postgres": "^3.4.8"
  }
}
